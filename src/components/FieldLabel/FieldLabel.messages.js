import { defineMessages } from 'react-intl';

const messages = defineMessages({
    optional: {
        id: 'optional',
        defaultMessage: 'Optional',
        description: 'Text if input field is optional',
    },
});
export default messages;
